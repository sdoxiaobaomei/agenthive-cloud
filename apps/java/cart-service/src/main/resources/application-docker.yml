server:
  port: 8085

spring:
  application:
    name: cart-service
  data:
    redis:
      host: redis
      port: 6379
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
