server:
  port: 8086

spring:
  application:
    name: logistics-service
  data:
    redis:
      host: agenthive-redis
      port: 6379
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
