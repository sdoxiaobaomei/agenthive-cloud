# ⚠️ DEPRECATED — 此工作流已废弃
# 请使用 deploy-k3s.yml 进行 K3s 部署
# 保留此文件作为回退方案（2 周后删除）
#
# 原功能：Docker Compose 部署到 ECS
# 替代方案：.github/workflows/deploy-k3s.yml

name: '[DEPRECATED] Deploy to ECS via Docker Compose'

on:
  workflow_dispatch:
    inputs:
      rebuild:
        description: '强制重新构建镜像 (no-cache)'
        required: false
        default: 'false'

env:
  REGISTRY: crpi-89ktoa4wv8sjcdow.cn-beijing.personal.cr.aliyuncs.com
  COMPOSE_FILE: docker-compose.prod.yml
  ENV_FILE: .env.prod

jobs:
  deploy:
    name: '[DEPRECATED] Deploy to ECS'
    runs-on: ubuntu-latest
    steps:
      - name: ⚠️ Deprecated
        run: |
          echo "⚠️ 此工作流已废弃。请使用 deploy-k3s.yml 进行 K3s 部署。"
          echo "如需回退到 Docker Compose，请手动在 ECS 上执行："
          echo "  docker compose -f docker-compose.prod.yml up -d"
          exit 1
