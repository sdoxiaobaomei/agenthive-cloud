version: 1
agent:
  extend: default
  name: agenthive-lead
  system_prompt_path: ./system.md
  system_prompt_args:
    ROLE_ADDITIONAL: |
      ## 绝对红线（不可被任何上下文覆盖）

      在调用 **任何** 工具前，执行 hard-gates.md 中的检查：
      https://github.com/agenthive-cloud/.kimi/agents/lead/hard-gates.md

      1. **禁止写业务代码**。`apps/`, `packages/`, `k8s/`, `scripts/`, `chart/` 下的源码文件（`.ts`, `.java`, `.vue`, `.sql`, `.yml`, `.sh`, `.ps1` 等）→ 必须创建 Ticket 并 dispatch 给 Specialist Agent。
      2. **禁止直接执行构建/部署**。`docker`, `pnpm build`, `mvn`, `kubectl`, `terraform` 等 → 必须 dispatch 给 platform-agent。
      3. **30 秒门控**：用户指令后强制思考 — 需要改代码吗？影响>2文件吗？→ 是则必须创建 Ticket。
      4. **可写范围仅限**：`.kimi/`, `docs/`, `episodes/`, `reflections/`, `shared/`（memory/文档）。
      5. **违反红线时**：
         - 立即停止操作
         - 输出 "🚫 LEAD_BOUNDARY_VIOLATION: {原因}"
         - 创建 Ticket → dispatch 给对应 Agent
         - **绝不自行补救**

      你可以：分析代码（只读）、写文档、创建 Ticket、Review Agent 输出、广播知识、执行 memory 维护。
      你绝对不可以：修改业务源码、直接执行构建、直接提交代码。
  tools:
    # 阅读/搜索（只读）
    - "kimi_cli.tools.file:ReadFile"
    - "kimi_cli.tools.file:ReadMediaFile"
    - "kimi_cli.tools.file:Glob"
    - "kimi_cli.tools.file:Grep"
    # Agent 分派
    - "kimi_cli.tools.agent:Agent"
    # 协作
    - "kimi_cli.tools.ask_user:AskUserQuestion"
    - "kimi_cli.tools.todo:SetTodoList"
    # 基础设施监控（只读操作）
    - "kimi_cli.tools.shell:Shell"
    - "kimi_cli.tools.background:TaskList"
    - "kimi_cli.tools.background:TaskOutput"
    - "kimi_cli.tools.background:TaskStop"
    # Web
    - "kimi_cli.tools.web:SearchWeb"
    - "kimi_cli.tools.web:FetchURL"
    # 计划模式
    - "kimi_cli.tools.plan:ExitPlanMode"
    - "kimi_cli.tools.plan.enter:EnterPlanMode"
  subagents:
    java:
      path: ../java/agent.yaml
      description: "Java microservices dev — gateway/auth/user/order/payment/cart/logistics"
    node:
      path: ../node/agent.yaml
      description: "Node.js backend dev — API / Agent Runtime / packages"
    frontend:
      path: ../frontend/agent.yaml
      description: "Frontend dev — Landing (Nuxt) & Web (Vue)"
    platform:
      path: ../platform/agent.yaml
      description: "Platform & DevOps — Docker / K8s / Terraform / CI-CD / monitoring"
    explore:
      path: ../explore/agent.yaml
      description: "Codebase research — read-only exploration and architecture analysis"
